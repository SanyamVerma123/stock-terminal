import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { LoadingState } from "@/components/ui/loading-state";
import type { HistoryPoint } from "@/lib/finance-types";
import { fmtPrice } from "@/lib/format";
export function PriceChart({ data, positive = true, currency = "USD" }: { data: HistoryPoint[]; positive?: boolean; currency?: string }) { const color = positive ? "#10b981" : "#f43f5e"; const points = data.filter(point => point.close !== null).map(point => ({ ...point, label: new Date(point.timestamp).toLocaleDateString("en-US", { month: "short", day: "numeric" }) })); if (!points.length) return <LoadingState label="Loading price history" detail="The market data provider is preparing this chart range."/>; return <div className="price-chart"><ResponsiveContainer width="100%" height={310}><AreaChart data={points} margin={{ top: 10, right: 6, left: -22, bottom: 0 }}><defs><linearGradient id="price-fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor={color} stopOpacity={.32}/><stop offset="100%" stopColor={color} stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="var(--chart-grid)" strokeDasharray="3 3"/><XAxis dataKey="label" minTickGap={48} tick={{ fontSize: 11, fill: "var(--muted-foreground)" }} axisLine={false} tickLine={false}/><YAxis domain={["auto", "auto"]} tickFormatter={value => fmtPrice(value, currency)} width={74} tick={{ fontSize: 11, fill: "var(--muted-foreground)" }} axisLine={false} tickLine={false}/><Tooltip formatter={(value: number) => fmtPrice(value, currency)} labelStyle={{ color: "var(--foreground)" }} contentStyle={{ background: "var(--card)", border: "1px solid var(--border)", borderRadius: 10 }}/><Area type="monotone" dataKey="close" stroke={color} strokeWidth={2.2} fill="url(#price-fill)" /></AreaChart></ResponsiveContainer></div>; }
