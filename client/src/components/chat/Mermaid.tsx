import { useEffect, useId, useState } from "react";
export function Mermaid({ code }: { code: string }) { const id = useId().replace(/:/g, ""); const [svg, setSvg] = useState(""); useEffect(() => { let active = true; import("mermaid").then(({ default: mermaid }) => { mermaid.initialize({ startOnLoad: false, theme: document.documentElement.classList.contains("dark") ? "dark" : "default", securityLevel: "strict" }); return mermaid.render(`chart-${id}`, code); }).then(result => { if (active) setSvg(result.svg); }).catch(() => { if (active) setSvg("<p>Diagram could not be rendered.</p>"); }); return () => { active = false; }; }, [code, id]); return <div className="mermaid-diagram" dangerouslySetInnerHTML={{ __html: svg }} />; }
