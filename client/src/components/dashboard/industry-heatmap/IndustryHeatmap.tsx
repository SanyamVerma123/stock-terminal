import type { FinanceQuote } from "@/lib/finance-types";
import { fmtPercent } from "@/lib/format";
export function IndustryHeatmap({ quotes }: { quotes: FinanceQuote[] }) { return <section className="panel heatmap"><div className="panel-heading"><div><span className="eyebrow">Market breadth</span><h2>Industry heatmap</h2></div><span className="microcopy">Price change</span></div><div className="heatmap-grid">{quotes.slice(0, 8).map(quote => { const value = quote.changePercent ?? 0; const intensity = Math.min(0.9, 0.15 + Math.abs(value) / 9); return <a key={quote.symbol} href={`/stock/${quote.symbol}`} className={`heat-cell ${value >= 0 ? "positive" : "negative"}`} style={{ "--heat": intensity } as React.CSSProperties}><b>{quote.symbol}</b><span>{quote.name}</span><strong>{fmtPercent(value)}</strong></a>; })}</div></section>; }
